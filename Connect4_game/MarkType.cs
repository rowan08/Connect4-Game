namespace Connect4_game
{
    /// <summary>
    /// Type of mark shown in each square - free (empty), nought (O) and cross (X)
    /// </summary>
    enum MarkType
    {
        Free,
        Nought,
        Cross
    }
}
